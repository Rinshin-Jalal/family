name = "family-plus-backend"
main = "src/index.ts"
compatibility_date = "2025-12-20"
compatibility_flags = [ "nodejs_compat" ]

[vars]
# Application configuration (non-sensitive)

[[r2_buckets]]
binding = "AUDIO_BUCKET"
bucket_name = "family-plus-audio"

# ----------------------------------------------------------------------------
# BACKGROUND PROCESSING
# ----------------------------------------------------------------------------
# Currently using synchronous processing (inline after response creation).
# Future: Integrate trigger.dev or similar for true async background jobs.
# ----------------------------------------------------------------------------

[observability]
enabled = true
